#ifndef DRAW_FUNCTIONS_H
#define DRAW_FUNCTIONS_H

#include "mem_type.h"         // define data types
#include "memory_access.h"    // define how to acces memory blocks

#include "bmp16_func.h"       // define primitives for bitmap 16bit vram generic
#include "m3_func.h"          // redefine primitives for mode 3 (16bit color, single page)
#include "m4_func.h"          // redefine primitives for mode 4 (8bit color index 16bit palette, double page)

#endif // DRAW_FUNCTIONS_H
